package com.example.quickcash;

import androidx.annotation.NonNull;

import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Rule;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.Mockito;
import org.mockito.junit.MockitoJUnitRunner;

import java.text.ParseException;
import java.util.concurrent.TimeUnit;

import static org.junit.Assert.assertEquals;


@RunWith(MockitoJUnitRunner.Silent.class)
public class startDateEndDateTest {

//interface

    @BeforeClass

    public static void setup(){
        //mockito for imputting a start date
        //mockito for inputting an end date
    }

    @Test
    public void testSendOffer() throws ParseException {
        //assertEquals (properdate, .getstartdate)
        //assertEquals (enddate, theendDEate)
        //Mockito.verify both the end and start dates
    }
}
